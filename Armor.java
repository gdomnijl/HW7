/**
	 * 
	 * The class Armor holds two fields that store the minimun and maximum defense value of the armor as strings
	 */
public class Armor {
	public String minVal; 
	public String maxVal; 

	public Armor (String minVal, String maxVal) {
		this.minVal = minVal; 
		this.maxVal = maxVal; 
	}
}
